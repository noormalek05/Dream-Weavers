function goToPage(page) {
    if (page === 'workout') {
        window.location.href = 'workout-plan.html'; // Redirect to workout plan page
    } else if (page === 'tracker') {
        window.location.href = 'fitness-tracker.html'; // Redirect to fitness tracker page
    }
}
